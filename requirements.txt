# Backend runtime dependencies for Python 3.14 and Django 6.0.
# Keep this file focused on direct dependencies; pip resolves transitive pins.
Django==6.0.7

# API and authentication
djangorestframework==3.17.2
django-cors-headers==4.9.0

# Frontend integration and PDF generation
django-webpack-loader==3.2.3
pdfkit==1.0.0

# Production runtime helpers
psycopg2-binary==2.9.11
python-dotenv==1.2.2
whitenoise==6.12.0
